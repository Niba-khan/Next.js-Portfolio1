/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['image.winudf.com','w0.peakpx.com', 'www.freecodecamp.org', 'i.ytimg.com', 'i.pinimg.com']
    }
};

export default nextConfig;
